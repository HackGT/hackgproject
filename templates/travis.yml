dist: trusty

{{#use_docker}}
sudo: required
services:
  - docker
{{/use_docker}}

script:
  - .travis.d/build.sh

env:
  global:
    - HACKGPROJECT_REV="{{source_rev}}"
{{#use_docker}}
    - DOCKER_ID_USER="{{org_name}}"
{{/use_docker}}

branches:
  only:
    - gh-pages
    - /.*/
